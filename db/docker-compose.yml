version: '3.1'

services:
  db:
    image: mcr.microsoft.com/mssql/server
    restart: always
    ports:
      - 1433:1433
    environment:
      ACCEPT_EULA: Y
      SA_PASSWORD: "Example!23"
    volumes:
      - "db-data:/var/opt/mssql"

volumes:
  db-data: